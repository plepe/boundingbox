{
  "name": "boundingbox",
  "description": "Implements bounding boxes with usual functions like intersects. Can convert from/to Leaflet bounds and GeoJSON.",
  "version": "0.1.4",
  "main": "BoundingBox.js",
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "geojson-bounds": "*",
    "haversine": "^1.1.1",
    "jsdoc": "^3.5.5",
    "standard": "^12.0.1"
  },
  "devDependencies": {
    "browserify": "*",
    "leaflet": "*",
    "mocha": "*"
  },
  "scripts": {
    "test": "mocha --bail && browserify test/test_*.js --debug -o all_tests.js",
    "build": "mkdir -p dist; browserify BoundingBox.js -o dist/boundingbox.js",
    "prepublish": "npm run build; npm run lint; npm run doc",
    "preversion": "npm run test",
    "version": "npm run build; npm run lint; npm run doc; git add doc/ ; git add -A dist/ doc/",
    "doc": "jsdoc -d doc/ BoundingBox.js",
    "lint": "standard --fix BoundingBox.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/plepe/boundingbox.git"
  },
  "keywords": [
    "bbox",
    "bounding-box",
    "leaflet",
    "GeoJSON",
    "bounds"
  ],
  "author": "Stephan Bösch-Plepelits",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/plepe/boundingbox/issues"
  },
  "homepage": "https://github.com/plepe/boundingbox#readme"
}
